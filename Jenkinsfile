pipeline {
    agent any

    options {
        timeout(time: 1, unit: 'HOURS') 
    }
    stages {
        stage('Testando o script comum') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
